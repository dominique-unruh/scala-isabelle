# Autogenerated from template.yml. Changes will be lost.

version: 2.1

orbs:
  windows: circleci/windows@2.4.0

workflows:
  version: 2
  linux2021:
    jobs:
      - test:
          filters:
            branches:
              only:
                - circleci-project-setup
                - master
                - release-candidate
                - test-macos
                - tmp

jobs:
  do_nothing_job:
    docker: [ image: cimg/base:2020.01 ]
    steps:
      - checkout

  test:

    machine: {image: ubuntu-2004:202010-01}

    steps:
      - checkout

      # Download and cache Isabelle
      - restore_cache:
          keys:
            - v3-isabelle-2021-RC3-linux
            - v2-isabelle-2021-RC3-linux
      - run:
          name: Downloading Isabelle 2021-RC3 (if needed)
          shell: bash
          command: |
            if ! [ -e "$HOME/install/Isabelle2021-RC3" ]; then
              mkdir -p ~/install
              case linux in
                windows)
                  curl "https://isabelle.sketis.net/website-Isabelle2021-RC3/dist/Isabelle2021-RC3_linux.tar.gz" -o /tmp/isabelle.exe
                  7z x -y -o"$HOME/install" /tmp/isabelle.exe;;
                linux|macos)
                  curl "https://isabelle.sketis.net/website-Isabelle2021-RC3/dist/Isabelle2021-RC3_linux.tar.gz" | tar -x -z -C ~/install;;
                *) false;;
              esac
            fi
      - save_cache:
          paths:
            - ~/install/Isabelle2021-RC3
          key: v3-isabelle-2021-RC3-linux

      # Download and cache dependencies
      - restore_cache:
          keys:
            - v3-dependencies-linux-213-{{ checksum "build.sbt" }}-{{ checksum "publish.sbt" }}
            - v3-dependencies-linux-213-
      - run:
          name: Downloading build dependencies (if needed)
          shell: bash
          command: |
            if ! sha1sum -c ~/dependencies-sha1sum; then
              mkdir -p ~/install
              curl -Ls https://git.io/sbt > ~/install/sbt
              chmod +x ~/install/sbt
              ~/install/sbt -213 update test/update </dev/null
            fi
            sha1sum build.sbt publish.sbt > ~/dependencies-sha1sum
      - save_cache:
          paths:
            - ~/install/sbt
            - ~/.m2
            - ~/.sbt
            - ~/.ivy2
            - $LOCALAPPDATA\Coursier\Cache
            - ~/.cache/coursier
            - ~/dependencies-sha1sum
          key: v3-dependencies-linux-213-{{ checksum "build.sbt" }}-{{ checksum "publish.sbt" }}

      - run:
          name: Running tests
          shell: bash
          command: |
            case linux in
              windows)
                # Isabelle process calls rebaseall and then fails, unless we deactivate it
                > ~/install/Isabelle2021-RC3/contrib/cygwin/isabelle/rebaseall
                cygpath -w ~/install/Isabelle2021-RC3 > .isabelle-home;;
              linux|macos)
                echo ~/install/Isabelle2021-RC3 > .isabelle-home;;
            esac

            ~/install/sbt -213 "testOnly -- -u scalatest-reports" </dev/null

      - store_test_results:
          path: scalatest-reports
